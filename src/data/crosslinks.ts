// Cross-app link URLs — centralized so all pages reference one source of truth.
// Override via VITE_COMMAND_CENTER_URL environment variable for per-deployment configuration.
export const COMMAND_CENTER_URL = import.meta.env.VITE_COMMAND_CENTER_URL || 'https://command-center-scotty123868s-projects.vercel.app';
